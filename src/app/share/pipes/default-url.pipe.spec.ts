import { DefaultUrlPipe } from './default-url.pipe';

describe('DefaultUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new DefaultUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
